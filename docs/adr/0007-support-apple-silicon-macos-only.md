# Support Apple Silicon macOS only

Strata's first macOS target will be Apple Silicon (`aarch64-apple-darwin`) alongside the existing Linux target. Intel macOS is outside this phase because there is no stated user need or validation environment; it can be added later if demand justifies another build and test target.
