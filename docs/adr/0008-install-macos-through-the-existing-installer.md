# Install macOS through the existing installer

The existing `install.sh` will detect Apple Silicon macOS, build the Rust CLI locally, install it into the vault, and support a smoke test. Homebrew packaging, prebuilt macOS artifacts, code signing, notarization, and Intel macOS distribution are outside this phase.
