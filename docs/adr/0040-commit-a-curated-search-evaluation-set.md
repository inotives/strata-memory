# Commit a curated search evaluation set

The synthetic fixture will include ten committed evaluation queries with expected top result paths. The set covers title, description, body, exact phrase, metadata filtering, archived exclusion, typo or no-match behavior, natural-language semantic retrieval, and mixed full-text/vector relevance so backend comparisons remain deterministic and reviewable.
