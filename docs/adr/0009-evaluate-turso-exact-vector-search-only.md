# Evaluate Turso exact vector search only

The Turso backend will use Turso's native exact vector representation and distance functions and will measure correctness and latency on a representative vault. Strata will not implement its own approximate-nearest-neighbor index; Turso's future vector-indexing capability can be evaluated separately when it is available and stable.
