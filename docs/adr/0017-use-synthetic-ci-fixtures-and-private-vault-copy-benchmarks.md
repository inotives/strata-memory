# Use synthetic CI fixtures and private vault-copy benchmarks

Backend contract and regression tests will use a small committed synthetic vault for deterministic CI. Scale and performance evaluation may additionally use an explicitly approved temporary copy of the private vault, but reports will contain only aggregate timings, counts, file sizes, stability results, and relevance metrics; private vault content and sensitive query text will not be published.
