# Require an explicit Turso evaluation verdict

The Phase 6 evaluation report must conclude with exactly one verdict: `promote-candidate`, `continue-evaluation`, or `reject-for-now`. It must identify the exact Turso version tested and every failed gate; qualitative conclusions such as “promising” are insufficient and the verdict does not itself change the default backend.
